import Steps from "./steps";
import TitleDescription from "./TitleDescription";

const HowWorking = () => {
  return (
    <section>
      <TitleDescription />
      <Steps />
    </section>
  );
};

export default HowWorking;
